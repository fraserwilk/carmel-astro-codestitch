---
import { Picture } from 'astro:assets';
import tile1Image from "@assets/images/pexels-photo-3754676.webp";
import tile2Image from "@assets/images/emdr-cp.jpg";
import tile3Image from "@assets/images/about me option B.webp";

---
<section id="services-333">
    <div class="cs-container">
        <div class="cs-content">
            <h2 class="cs-title">More Information</h2>
        </div>
        <ul class="cs-card-group">
            <li class="cs-item">
                <a href="/work-with-me" class="cs-link">
                    <Picture class="cs-picture" src={tile1Image} formats={['webp', 'jpeg']} alt="Client base image" loading="lazy" />
                        
                    <h3 class="cs-h3">My client base</h3>
                    <div class="cs-item-text">I have success in working with clients who are:</div>
                    <ul class="cs-item-text" style="padding-left:2.7rem;">
                        <li>First Responders, eg. NSW Police Force, NSW Ambulance, Fire & Rescue NSW</li>
                        <li>Experiencing trauma or are feeling 'stuck'</li>
                        <li>Dealing with relationship breakdowns/family changes</li>
                        <li>NDIS clients</li>
                        <li>Relocating to new work/living areas</li>
                        <li>CTP insurance</li>
                    </ul>
                    <p class="cs-item-text cs-item-readmore">Read more...</p>
                    
                </a>
            </li>
            <li class="cs-item">
                <a href="/emdr" class="cs-link">
                    <Picture class="cs-picture" src={tile2Image} formats={['webp', 'jpeg']} alt="EMDR expertise" loading="lazy" />
                    <h3 class="cs-h3">EMDR - what is it?</h3>
                    <p class="cs-item-text cs-justify">
                        Eye Movement Desensitisation and Reprocessing (EMDR) Therapy is an evidence-based therapy and has been used in a variety of settings and countries over the last 30 years.<br />
                        <br />

                        <ul class="cs-item-text" style="padding-left:2.7rem;">
                            <li>You don't have to talk about your trauma in detail</li>
                            <li>It doesn't require homework</li>
                            <li>It often produces quicker results</li>
                            <li>The benefits continue long after treatment has finished</li>
                        </ul>

                        <p class="cs-item-text cs-item-readmore">Read more...</p>
                    </p>
                </a>
            </li>
            <li class="cs-item">
                <a href="../about/" class="cs-link">
                    <Picture class="cs-picture" src={tile3Image} formats={['webp', 'jpeg']} alt="About me" loading="lazy" />
                    
                    <h3 class="cs-h3">About me</h3>
                    <p class="cs-item-text">
                        <i>Australian professional journey:</i><br />
                        Melbourne, Perth, Sydney and rural NSW<br />
                        <i>International professional experience:</i><br />
                        Lived and worked as a psychologist in Vietnam for 3 years.<br />
                        <i>Rural professional experience:</i><br />
                        Successful private practice in the Southern Highlands for over a decade.<br />
                        <br />
                        <p class="cs-item-text cs-item-readmore">Read more...</p>
                    </p>
                </a>
            </li>
        </ul>
    </div>
  </section>

  <style lang="less">

/* Mobile - 360px */
@media only screen and (min-width: 0rem) {
    #services-333 {
        padding: var(--sectionPadding);
        border-top: double 4px var(--primaryLight);
        border-bottom: double 4px var(--primaryLight);
        /* background-color: var(--primaryLight); */

        .cs-container {
            width: 100%;
            max-width: (1280/16rem);
            margin: auto;
            display: flex;
            flex-direction: column;
            align-items: center;
            /* 48px - 64px */
            gap: clamp(3rem, 6vw, 4rem);
        }

        .cs-content {
            /* set text align to left if content needs to be left aligned */
            text-align: center;
            width: 100%;
            display: flex;
            flex-direction: column;
            /* centers content horizontally, set to flex-start to left align */
            align-items: center;
        }

        .cs-card-group {
            width: 100%;
            padding: 0;
            margin: 0;
            display: flex;
            justify-content: center;
            align-items: stretch;
            flex-direction: column;
            gap: clamp(1rem, 1.6vw, 1.25rem);
        }

        .cs-item {
            list-style: none;
            width: 100%;
            max-width: 31.25rem;
            border-radius: 8px;
            position: relative;
            z-index: 1;
            transition:
                box-shadow 0.3s,
                transform 0.3s;

            &:hover {
                box-shadow: rgba(149, 157, 165, 0.2) 0px 8px 24px;
                transform: translateY(-0.4375rem);
                .cs-picture {
                    img {
                        opacity: 0.3;
                        transform: scale(1.1);
                    }
                }
                .cs-h3 {
                    color: var(--secondaryLight);
                }
            }

            &:before {
                /* border, done in pseudo so it can sit under the picture */
                content: "";
                width: 100%;
                height: 100%;
                border-radius: (8/16rem);
                border: 1px solid #e8e8e8;
                /* prevents border from affecting height and width */
                box-sizing: border-box;
                background: transparent;
                opacity: 1;
                position: absolute;
                display: block;
                top: 0;
                left: 0;
                z-index: -1;
            }
        }

        .cs-link {
            text-decoration: none;
            /* 16px - 24px */
            padding-bottom: clamp(1rem, 2.2vw, 1.5rem);
            display: block;
        }

        .cs-picture {
            width: 100%;
            height: 15rem;
            /* 16px - 24px */
            margin-bottom: clamp(1rem, 2.2vw, 1.5rem);
            border-radius: (8/16rem) (8/16rem) 0 0;
            background-color: var(--primaryLight);
            /* clips the img tag corners */
            overflow: hidden;
            display: block;
            position: relative;
            z-index: 10;
            object-fit: cover;
            object-position: 30% 10%;

            img {
                position: absolute;
                top: 0;
                left: 0;
                height: 100%;
                width: 100%;
                object-fit: cover;
                transition:
                    transform 0.6s,
                    opacity 0.3s;
            }
        }

        .cs-h3 {
            /* 20px - 25px */
            font-size: clamp(1.25rem, 3vw, 1.5625rem);
            line-height: 1.2em;
            font-weight: 900;
            color: var(--headerColor);
            margin: 0 0 (12/16rem) 0;
            /* 16px - 24px */
            padding: 0 clamp(1rem, 2vw, 1.5rem);
            transition: color 0.3s;
        }

        .cs-item-text {
            /* 14px - 16px  */
            font-size: clamp(0.875rem, 1.5vw, 1rem);
            line-height: 1.5em;
            color: var(--bodyTextColor);
            margin: 0;
            /* 16px - 24px */
            padding: 0 clamp(1rem, 2.2vw, 1.5rem);
        }

        .cs-justify {
            text-align: justify;
        }

        .cs-item-readmore {
            padding-top: 1rem;
            color: var(--primary);
        }
    }
}

/* Tablet - 768px */
@media only screen and (min-width: 48rem) {
    #services-333 {
        .cs-card-group {
            flex-direction: row;
            flex-wrap: wrap;
        }

        .cs-item {
            /* we do this so it's stackable. You can add any number of reviews you want and they will stack and center in the middle. We don't use grid because if you have an odd number of cards, they don't stay centered, they align with their grid lines. This way its more FLEX-ible*/
            width: clamp(31.85%, 30.5vw, 32.28%);
        }
    }
}

/* Dark Mode */
@media only screen and (min-width: 0rem) {
    body.dark-mode {
        #services-333 {
            .cs-h3,
            .cs-item-text,
            .cs-item-readmore {
                color: var(--bodyTextColorWhite);
            }

            .cs-item-text,
            .cs-item-readmore {
                opacity: 0.8;
            }

            .cs-background {
                background-color: rgba(44, 81, 38, 1.0);
            }
        }
    }
}
                                
  </style>